<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Location permissions. These merge into the consuming app's manifest, so only
         what this plugin genuinely needs is declared (least privilege). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground service (Android 14+ additionally requires the typed permission) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Work-hour uploads -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Geofence crossings post a local notification from a process that may have
         been started solely to deliver the broadcast. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <!-- Task tracking service -->
        <service
            android:name=".BackgroundLocationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Work hour tracking service -->
        <service
            android:name=".WorkHourLocationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Geofence crossings. Manifest-registered, not runtime-registered: the
             whole point is to be reachable when the app is not running, and Android
             starts a fresh process for this broadcast. -->
        <receiver
            android:name=".GeofenceBroadcastReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.elsapiens.backgroundlocation.GEOFENCE_TRANSITION" />
            </intent-filter>
        </receiver>

        <!-- Restart watchdog (explicit PendingIntent broadcasts, so manifest
             registration works even when the process is dead) -->
        <receiver
            android:name=".ServiceRestartReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.elsapiens.backgroundlocation.RESTART_SERVICE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
